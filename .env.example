# Postgres и Lightdash (обязательно для docker-compose и генерации данных)
# Для учебного проекта можно оставить так. Если меняете пароль — обновите также подключение в Cursor MCP.
PGPASSWORD=postgres
# Должно быть 32+ символа (можно оставить как есть для локального запуска).
LIGHTDASH_SECRET=local_lightdash_secret_change_me_32_chars

# dbt project path (абсолютный или относительный; монтируется в Lightdash)
DBT_PROJECT_DIR=./dbt_bi

# Подключение к Postgres с хоста (скрипт генерации данных, dbt). Для Lightdash в Docker используется PGHOST=db в docker-compose.
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGDATABASE=postgres

# Optional: Lightdash
PORT=8080
SITE_URL=http://localhost:8080
