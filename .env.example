# Postgres и Lightdash (обязательно для docker-compose и генерации данных)
PGPASSWORD=your_postgres_password
LIGHTDASH_SECRET=your_lightdash_secret_at_least_32_chars

# dbt project path (абсолютный или относительный; монтируется в Lightdash)
DBT_PROJECT_DIR=./dbt_bi

# Подключение к Postgres с хоста (скрипт генерации данных, dbt). Для Lightdash в Docker используется PGHOST=db в docker-compose.
# PGHOST=localhost
# PGPORT=5432
# PGUSER=postgres
# PGDATABASE=postgres

# Optional: Jira MCP
# JIRA_URL=https://your-domain.atlassian.net
# JIRA_EMAIL=your@email.com
# JIRA_API_TOKEN=your_api_token

# Optional: Lightdash
PORT=8080
SITE_URL=http://localhost:8080
